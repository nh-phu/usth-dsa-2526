/*
 - We can use a stack structer to push from Head to end of the this single linked list.
 then display elements with pop so that it display in reverse order.
 - The basic functions are init the stack, push elements from the list to the stack,
 pop and display each element.
 - O(2n)
*/
